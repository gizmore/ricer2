en:
  ricer:
    plugins:
      ai:

        action:
          trigger: 'action'
          description: 'Sends an action to a user or channel.'

        echo:
          trigger: 'echo'
          description: 'Replies the text you give as parameter.'

        message:
          trigger: 'send'
          description: 'Sends a message to a user or channel, which respects user preferences of notice or privmsg.'

        notice:
          trigger: 'notice'
          description: 'Sends a notice to a user or channel.'

        say:
          trigger: 'say'
          description: 'Sends a privmsg to a user or channel.'
